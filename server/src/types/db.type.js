const GenderTypes = Object.freeze({
    MALE: "male", 
    FEMALE: "female", 
    OTHER: "other",
})

const RoomTypes = Object.freeze({
    SINGLE: "single", 
    GROUP: "group", 
    ALL: "all", 
})

module.exports = {
    GenderTypes, 
    RoomTypes, 
}