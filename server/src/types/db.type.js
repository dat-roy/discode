const GenderTypes = Object.freeze({
    MALE: "male", 
    FEMALE: "female", 
    OTHER: "other",
})

const ConversationTypes = Object.freeze({
    SINGLE: "single", 
    GROUP: "group", 
    ALL: "all", 
})

module.exports = {
    GenderTypes, 
    ConversationTypes, 
}